# requirements-minimal.txt
# Minimal dependencies for ARM systems (Apple Silicon, ARM servers, etc.)
# OpenSearch Korean Neural Sparse Model - Core Dependencies Only

# Core ML frameworks
torch>=2.0.0
transformers>=4.40.0
datasets>=2.0.0
tokenizers>=0.15.0
safetensors>=0.4.0

# Data science essentials
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Progress and utilities
tqdm>=4.65.0

# Phase 1: Loss functions
# (No additional dependencies - PyTorch built-ins)

# Phase 2: Temporal analysis
# (No additional dependencies - uses stdlib datetime)

# Phase 3: Hard negative mining
rank-bm25>=0.2.2

# Phase 4: Clustering
scipy>=1.10.0

# Configuration
pyyaml>=6.0

# Optional: Visualization
# matplotlib>=3.7.0
# seaborn>=0.12.0

# Optional: Jupyter
# jupyter>=1.0.0
# notebook>=7.0.0

# ❌ NOT INCLUDED for ARM compatibility:
# - konlpy (requires Java dependencies)
# - mecab-python3 (C++ compilation issues on ARM)
# - hdbscan (optional clustering, can fail on ARM)

# requirements-monitoring.txt
# Monitoring dependencies for ML training

# System metrics
psutil==5.9.6

# GPU metrics (optional - requires NVIDIA GPU)
gputil==1.4.0

# Prometheus exporter (optional - for Phase 3)
prometheus-client==0.19.0

# Flask for web dashboard (optional - for Phase 5)
flask==3.0.0
